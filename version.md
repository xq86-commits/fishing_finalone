# Version 6.3

- 音乐按钮默认自动关闭，进入首页或游戏时不再自动播放，需要用户手动开启。

# Version 6.2

- 使用 Hint 道具也会奖励 100 分，鼓励合理使用提示。
- 首页隐藏 Leaderboard 按钮（保留功能入口但默认不展示），Share 按钮上移。

# Version 6.1.3

- 修复广告回调跨 iframe 的访问问题：Hint 按钮调用与监听统一从父页面或当前页读取 advertisingManager，避免线上出现 “Ad unavailable”。

# Version 6.1.2

- Hint 角标保持 80% 尺寸，并左移覆盖部分灯泡图标以更明显提醒。

# Version 6.1.1

- Hint 角标移至灯泡图标右上角叠加显示，更符合习惯位置。

# Version 6.1

- Hint 角标缩小至原先的 80%，背景改为红色，角标文字同步缩小。
- Hint 图标下方文案保持 “Hint” 不再显示括号与数量。

# Version 6.0

- Hint 道具支持看激励视频补充：初始 3 次，用完后灯泡角标显示 “AD”，点击调用原生广告，观看完成自动补充 Hint +1 并提示。
- Hint 按钮新增角标展示剩余次数/AD，配合广告回调浮动提示。

# Version 5.6

- Settings 的 Using 区域隐藏了日语选项（学习语言固定为日语），仅可选择其他语言查看释义。
- 上一版精简后的弹窗布局保持不变。

# Version 5.5

- Settings 弹窗去掉 Learning Language 选择项，学习语言固定为日语；仅保留 JLPT 等级与 Using 语言选择。
- 上移 Using 区域与 Apply 按钮并缩短弹窗高度，确保布局更紧凑。
- 词库加载保持不变，默认学习语言仍为日语。

# Version 5.4

- 为 JLPT N1~N4 增加种子词库（来自 n1~n4 参考文件），填入游戏内多语言 fallback，保证高级等级也能有基础释义。
- 合并全部等级种子到翻译映射中，缺失语言自动回退英文，便于后续完善。
- N1~N4 每个种子词库固定 60 条，保证不同等级初始词表丰富度一致。

# Version 5.3

- Settings 面板新增 JLPT 语言等级选择（N5~N1，默认 N5），实时展示当前使用等级。
- 词库接入 n1~n4（含 n5 扩展），为中文 / 韩语 / 西语 / 法语填充释义字段，缺失时自动回退英文保证多语言可读。
- 切换等级时支持词库加载提示并立即刷新当前出题，便于快速试用不同难度。

# Version 5.2

- 新增语言切换按钮（Lang），可在悬浮窗中选择学习语言与基础语言（支持中文、日语、韩语、英文、西班牙语、法语）。
- 悬浮窗确认时提示「开发中」，为后续接入词库预留入口。
- Lang/Hints/Note 按钮均位于 Lives 下方同一行，便于操作。

# Version 5.1.1

- 连续缺失的假名下划线用空格分隔，避免连成一条线。
- Hint / Note 按钮移至 Lives 下方，便于分组查看。

# Version 5.1

- 保持英文释义原样展示，仅对日语填空行按假名数量拆分下划线。
- 增加 Hint 按钮（左侧，3 次机会），点击自动填充一个缺失的假名。
- 增加 Note 按钮（右侧），点击弹出悬浮窗，展示已收集单词（含日语与英文释义）。
- 小鱼尺寸缩小至原来的 80%，鱼上方假名字体放大为原来的 1.2 倍。

# Version 5.0

- 语言输入框根据假名输入框的字符数拆分显示对应数量的下划线，提示位与假名位置一一对应。

# Version 4.9

- 视觉优化：结算页标题由 “Congratulations” 改为 “Your Score: xxx”，直接展示本局得分。

# Version 4.8.2

- 视觉优化：current vocab 底框内的单词与英文释义字体缩小至 80%，并居中展示。

# Version 4.8.1

- 布局优化：current vocab 区域增加半透明底框，单词与英文释义在同一块背景中展示。

# Version 4.8

- 布局优化：将日语词汇与英文释义移到人物左侧，避免顶部拥挤并贴合人物区域。

# Version 4.7

- 兼容性优化：完善了 Web Speech API 在移动端（WebView）的支持。
  - 增加了语音资源预加载和动态选择日语发音的逻辑。
  - 增加了首次交互时的静音解锁机制，解决部分移动设备音频无法自动播放的问题。
  - 修复了语音对象可能被垃圾回收导致播放中断的问题。

# Version 4.6

- 视觉优化：游戏内顶部 UI 文字颜色恢复为黑色 (#333)，去除阴影，保持清晰简洁。
- 视觉优化：小鱼身上的假名文字颜色调整为白色 (#fff)，提升在气泡背景下的对比度。

# Version 4.5

- 视觉优化：统一了首页和结算页按钮的宽度和样式。
- 视觉优化：游戏内顶部 UI 文字颜色统一为浅蓝色 (#E0F7FA) 并增加阴影，提升可读性。
- 视觉优化：小鱼气泡采用半透明磨玻璃效果，视觉更轻盈。

# Version 4.4

- 听觉体验：新增点击假名播放对应发音的功能（使用 Web Speech API）。

# Version 4.3

- 视觉优化：点击错误的假名时，小鱼会原地震动并渐隐消失，随后在屏幕边缘重新生成，增强错误反馈。

# Version 4.2

- 视觉优化：更新了游戏标题为 "Words Fishing"，并调整了主色调。
- 视觉优化：背景渐变调整为更清新的海蓝色 (#AEE2FF → #6BB9FF)。
- 视觉优化：按钮颜色和阴影样式更新，区分主次按钮。

# Version 4.1

- 功能新增：支持在结算页面点击 "Play Again" 按钮重新开始游戏。

# Version 4.0

- 体验优化：游戏结束时（时间耗尽或生命耗尽）增加 0.5 秒的延迟，并显示提示信息（"Time Over" 或 "Out of Lives"），避免突然结束带来的突兀感。

# Version 3.9

- 视觉优化：新增分数和生命值变化的浮动文字动画（+100 / -1）。
- 视觉优化：新增分数和生命值数字的滚动更新效果。

# Version 3.8

- 修复：修复了生命值未正确初始化（默认3）的问题。
- 修复：修复了单词拼写完成后游戏卡死（无法进入下一词）的 Bug。

# Version 3.7

- 视觉优化：移除生命值的红心图案，改为直接显示数字（如 Lives: 3），保持界面简洁。

# Version 3.6

- 视觉优化：修正小鱼游动方向，使其与移动方向一致。
- 视觉优化：使用红色心形图案绘制生命值，替代之前的文字显示。
- 游戏平衡：小鱼数量减少至 6 条（原数量的 75%），降低视觉干扰。

# Version 3.5

- 修复：修复了 index.html 内容丢失导致页面空白的问题。

# Version 3.4

- 游戏机制优化：新增生命值系统（3点），错误扣分，生命值耗尽游戏结束。
- 结算页面升级：游戏结束后展示本局找到的单词列表（包含日文和英文释义）。
- 交互优化：游戏结束逻辑调整，统一使用主页面的结算弹窗。

# Version 3.3

- 视觉优化：Canvas 适配高分屏，解决手机端文字模糊问题。
- 动画效果：小鱼增加上下浮动动画，更生动。
- 交互优化：单词完成后，旧鱼淡出消失，新鱼从屏幕两侧游入。

# Version 3.2

- UI 调整：移除顶部白色背景，整体信息下移 80px，优化视觉体验。
- 视觉优化：缩小气泡与小鱼的间距，使其更紧凑。
- 玩法升级：改为填空模式，随机展示部分假名，玩家需补充剩余假名（显示下划线提示）。

# Version 3.1

- 视觉优化：假名显示在小鱼上方的气泡中。
- 角色调整：人物背景透明化，并放置在海平面上。
- 游戏逻辑：小鱼仅在海平面以下游动。
- 难度调整：小鱼游动速度降低为原来的 75%。

# Version 3.0

- 全新玩法：日语钓鱼游戏。
- 游戏背景更换为海洋场景，角色为钓鱼人物。
- 核心玩法：在90秒内通过点击携带正确假名的小鱼来拼写单词。
- 单词库：接入 N5 词汇表，展示日语单词及英语释义。
- 视觉效果：小鱼在海面下左右游动，速度随机；人物在海面上晃动。

# Version 2.16

- 棋盘行数 ≥ 7 时，画布由正方形改为 1:1.2 长方形（纵向更高），保证字母布局不拥挤；同时继续使用 `cellSize * 0.4` 的字号。
- 修复新高度逻辑引起的布局计算错误，确保棋盘能够正常绘制并保留工具按钮。

# Version 2.15

- Moments 分享卡片统一改用 `shareimg.png`（`MOMENT_CARD_IMAGE`），确保朋友圈展示始终使用大图。

# Version 2.14

- 分享到 chat 时统一使用链接卡片（URL/ICON/TITLE/DESCRIPTION），分享至 Moments 则调用带 `goto_url` 与 `pic_url` 的卡片配置，保持与参考实现一致。

# Version 2.13

- 新增 LEVEL 5 ~ LEVEL 10 关卡配置，覆盖更多主题与词库，延长游戏体验。

# Version 2.12

- 关卡最后一个词被找到后延迟 0.5 秒再通知外层结算，让玩家有时间看到完成提示。

# Version 2.11

- Loading 文案统一为 `loading` 并仅展示 0.2 秒，排行榜抬头改为 `Leaderboard`。
- 分享得分文案更新为 “I just finished "level x" in this game, Can you beat me?”，并在分享请求中传递关卡信息。
- 所有分享都会优先调用 Learn Card 图片+链接方案，同时在 payload 中写入 `learn_card` 字段，满足 Moments 卡片需求。

# Version 2.10

- Target Words 顶部橙色横幅仅在上缘保留圆角，下缘改为直线以与内容区域自然连接。

# Version 2.9

- Target Words 区域与棋盘面板改为淡蓝渐变底色，与整体背景更统一、对比更柔和。

# Version 2.8

- Loading 时长缩短至 0.5 秒，尽快切换到游戏画面。
- 分享配置统一使用官方地址与图标，新增 Moments 专用 Learn Card 分享接口及占位图。

# Version 2.7

- 首页英文化：标题改为 WordSearch，按钮更新为 `start game`、`leaderboard`、`share`，匹配整体体验。
- 移除所有返回主页按钮（iframe 顶部 Back、外层返回主页入口），改由弹层按钮回到首页。
- 结算页使用 `Congratulations!` 副本，仅保留下一关/分享/Home 操作，去掉得分展示模块。
- 游戏 Canvas 去除 Back 按钮绘制与逻辑，专注关卡信息与目标提示。

# Version 2.6

- 棋盘字母加粗以与 Target Words 样式一致，突出当前任务词。
- 调整连线绘制位置，与字母同向上偏移，确保颜色覆盖上下均衡。

# Version 2.5

- Target Words 顶部橙色标题条与面板上边缘对齐，保持区域整体紧凑。
- 调整棋盘字母排布，使选中填色在字母上下方留白更均衡。
# Version 2.4

- 调整 Target Words 区域与背景宽度一致，缩短标题条与整体高度，并统一较小字号以提升紧凑性。
- 目标单词命中后仅置灰显示，不再使用删除线效果。
- 棋盘字母与连线宽度缩小至约 70%，维持高亮填充比例一致。
- Hint / Reveal / Rotate 三个道具按钮缩小尺寸，保持更轻量的底部操作区。

# Version 2.3

- 完善 Word Search Canvas：返回按钮改为 `Back`，移除顶部 Score，并为目标单词标题新增橙色底条 `#F36900` 与居中加粗的 `Target Words` 文案。
- 目标单词改为最多四列的居中布局，优化行高以适配不同关卡数量。
- 调整棋盘视觉，去除网格线，缩小字母字体与连线粗细，保持选区填充比例一致。
- 精简道具区域，按钮文本更新为 `Hint`、`Reveal`，并新增 `Rotate` 按钮，整体缩小尺寸。

# Version 2.2

- 更新首页、排行榜背景为与 Word Search Canvas 一致的蓝色渐变，并统一主按钮为橙色 `#F36900`。
- 调整结算弹层半透明背景与按钮配色，确保整体视觉风格一致。

# Version 2.1

- 调整 Word Search Canvas 布局，统一 Level 顶部与道具底部的留白，让主体区域整体下移并保持上下间距一致。

# Version 2.0

- 新增「Word Search」玩法，使用 Canvas 绘制布局并在手机屏幕上自适应显示。
- 支持滑动直线连线查找单词，拖动与完成后都会填充同色连线并叠加保留正确答案。
- 新增返回按钮、目标单词面板与道具占位按钮，保留通关消息上报并接入前两关配置。

# Version 1.4

- 排行榜默认头像统一使用 `assets/handy.png`，在无头像数据时自动回退。

# Version 1.3

- 游戏页 Canvas 改为全屏展示并去除圆角阴影，保持高清绘制。
- 结算烟花动画连续触发两次并支持中途清理，避免残留。
- 新增 `scripts/game-data.js`，统一处理排行榜获取与成绩上报，兼容无 API 地址时的默认数据。
- 排行榜 UI 支持头像、昵称、VIP 标识与成绩展示，按服务端规则排序并接入 API 数据。
- 关卡完成时构建上报数据并通过数据服务发送，保留回退逻辑。

# Version 1.2

- 新增 `scripts/fireworks.js`，提供可重复调用的全屏烟花动画并适配高分屏。
- 在 `index.html` 通关时触发烟花效果，切换页面时自动清理。

# Version 1.1

- 将分享逻辑抽离至 `scripts/share.js`，全量使用英文文案并保持原生/H5 双通道。
- 新增 `scripts/app-communication.js`，统一处理 App ⇄ H5 消息、分享及广告请求。
- 新增 `scripts/advertising-manager.js`，封装原生广告调用、回调与次数统计。
- 更新 `index.html`，加载新脚本、回传关键事件（开始游戏/分享/结算）并预加载广告。

## Version 1.0

- 更新首页分享按钮，接入原生 App 分享能力并提供 H5 回退方案。
- 更新结算页分享按钮，支持分享当前成绩并兼容原生与浏览器分享。
- 新增分享桥接配置，复用 HelloTalk iOS/Android 分享接口结构，便于后续扩展。
